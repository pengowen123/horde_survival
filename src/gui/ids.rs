widget_ids! {
    pub struct Ids {
        // Common background
        background,

        // Main menu
        button_new_game,
        button_options,
        button_quit,
        main_menu_image,
    }
}
