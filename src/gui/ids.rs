//! Widget ids

widget_ids! {
    /// A struct for unique widget ids
    pub struct Ids {
        // Common background
        background,

        // Main menu
        button_options,
        button_quit,
        main_menu_image,

        // New game menu
        button_new_game,

        // Shop menu
        canvas_items,
        scrollbar_items,
        matrix_items,
        button_next_round,
    }
}
